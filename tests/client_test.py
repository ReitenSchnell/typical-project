import so_client
import pytest_mock

tag = 'python'


# def test_returns_tags_inside_items_of_response(mocker):
    # mocker.patch.object(response, 'json', return_value={'foo'})
    # requests_mock = mocker.patch('requests.get')
    # requests_mock.return_value = {}
    # result = so_client.get_questions_by_tag(tag)
    # assert result == ''
